#include <stdexcept>
#include "ascii_art.h"
#include "ascii_text.h"

/*
 de-comment this when implementing
void ascii_text::add(ascii_art *art) {
    if (!arts_.empty() && art->height() != arts_.back()->height()) {
        throw std::invalid_argument("added art is of wrong height");
    }
    arts_.push_back(art);

}*/

